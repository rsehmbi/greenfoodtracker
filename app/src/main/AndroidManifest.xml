<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.t.teamten.greenfoodtracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultScreenFirst" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".aboutactivity" />
        <activity android:name=".FactsActivity" />
        <activity android:name=".ResultScreenSecond" />
        <activity android:name=".FirebaseLogin" />
        <activity android:name=".NewUserRegisteration" />
        <activity android:name=".UserRegisteration" />
        <activity android:name=".PledgeInteract"></activity>
    </application>

</manifest>